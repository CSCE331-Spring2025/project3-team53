{
  "name": "your-app",
  "version": "1.0.0",
  "scripts": {
    "start": "npm-run-all --parallel server client",
    "server": "cd server && node index.js",
    "client": "cd client && npm start"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "run-all": "^1.0.1"
  }
}
